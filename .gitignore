# Claude Code
.claude/
.epic/
.playwright-mcp/

# Session leftovers: an overlay never has HTML at the root.
# `bentoo overlay add` without paths is `git add .`, so anything sitting here
# when it runs gets committed and pushed.
/*.html
